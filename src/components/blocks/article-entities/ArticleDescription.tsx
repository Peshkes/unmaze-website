import React from 'react';

const ArticleDescription = ({text}: { text: string }) => {
    return (
        <div>
            <p>{text}</p>
        </div>
    );
};

export default ArticleDescription;
